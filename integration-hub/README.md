# Integration Hub

Constitutional boundary service exposing MCP capability discovery and governed hypermedia process execution.

## Endpoints

- GET /health
- GET /mcp/functions
- GET /mcp/functions/:id
- GET /process/:entity/:id
- POST /process/:entity/:id/actions/:action

## Development

- npm install
- npm run dev
