name: Release

on:
  workflow_dispatch:

jobs:
  build-and-tag-images:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder release step
        run: |
          echo "Release workflow stub is active."
          echo "Add image build and tagging steps when deployment targets are finalized."
